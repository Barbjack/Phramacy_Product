<UserControl x:Class="Phramacy_Product.Views.Components.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Phramacy_Product"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             Height="50">

    <UserControl.Resources>
        <DropShadowEffect x:Key="DropShadow" BlurRadius="8" ShadowDepth="2" Direction="270" Color="Black" Opacity="0.2"/>
        <Style x:Key="TopLevelMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Margin" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                            <Popup Name="Popup"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           IsOpen="{TemplateBinding IsSubmenuOpen}"
                           PopupAnimation="Fade">
                                <Border Background="White"
                                CornerRadius="0"
                                Padding="5"
                                HorizontalAlignment="Right"
                                Effect="{StaticResource DropShadow}">
                                    <StackPanel>
                                        <ItemsPresenter/>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SubMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Background="{TemplateBinding Background}" 
                     CornerRadius="5" 
                     Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#f0f0f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <materialDesign:ColorZone Background="#0b05ad" DockPanel.Dock="Top" Height="50">
        <DockPanel>
            <!-- Left-aligned main menu -->
            <Menu Background="Transparent" Height="50" VerticalAlignment="Top" HorizontalAlignment="Left">
                <MenuItem Click="Home">
                    <MenuItem.Header >
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Home" Width="20" Height="20" Foreground="White" Margin="0,0,5,0"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
                <!-- Sales -->
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}">
                    <MenuItem.Header>
                        <Grid Background="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Dollar" Width="20" Height="20" Foreground="White" Margin="0,0,5,0"/>
                                <TextBlock Text="Sales" Foreground="White" FontWeight="Bold"/>
                                <materialDesign:PackIcon Kind="ChevronDown" Width="16" Height="16" Foreground="White" Margin="5,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </MenuItem.Header>
                    <MenuItem Header="Sale Invoices" Click="NavigateToSaleInvoices" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Payment In" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Sale Order" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Sale Return" Style="{StaticResource SubMenuItemStyle}"/>
                </MenuItem>
                <!-- Inventory -->
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}"
          Click="NavigateToInventory">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Warehouse" Width="20" Height="20" Foreground="White" Margin="0,3,7,0"/>
                            <TextBlock Text="Inventory" Foreground="White" FontWeight="Bold" Margin="2,5,0,5"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>

                <!-- Reports -->
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ChartBar" Width="20" Height="20" Foreground="White" Margin="0,0,5,0"/>
                            <TextBlock Text="Reports" Foreground="White" FontWeight="Bold"/>
                            <materialDesign:PackIcon Kind="ChevronDown" Width="16" Height="16" Foreground="White" Margin="5,0,0,0"/>
                        </StackPanel>
                    </MenuItem.Header>
                    <MenuItem Header="Daily Report" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Monthly Report" Style="{StaticResource SubMenuItemStyle}"/>
                </MenuItem>

                <!-- More -->
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="EllipsisHorizontal" Width="20" Height="20" Foreground="White"  Margin="0,0,5,0"/>
                            <TextBlock Text="More" Foreground="White" FontWeight="Bold"/>
                            <materialDesign:PackIcon Kind="ChevronDown" Width="16" Height="16" Foreground="White" Margin="5,0,0,0"/>
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
            </Menu>

            <!-- Right-aligned user menu -->
            <Menu Background="Transparent" Height="50" VerticalAlignment="Center" HorizontalAlignment="Right">
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}" VerticalAlignment="Center" Margin="0,7,20,0">
                    <MenuItem.Header>
                        <materialDesign:PackIcon Kind="Magnify" Width="24" Height="24" Foreground="White"/>
                    </MenuItem.Header>
                    <MenuItem Header="Search something..." Style="{StaticResource SubMenuItemStyle}"/>
                </MenuItem>
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}" VerticalAlignment="Center" Margin="0,7,20,0">
                    <MenuItem.Header>
                        <materialDesign:PackIcon Kind="BellOutline" Width="24" Height="24" Foreground="White"/>
                    </MenuItem.Header>
                    <MenuItem Header="No new notifications" Style="{StaticResource SubMenuItemStyle}"/>
                </MenuItem>
                <MenuItem Style="{StaticResource TopLevelMenuItemStyle}" VerticalAlignment="Center" Margin="0,7,20,0">
                    <MenuItem.Header>
                        <materialDesign:PackIcon Kind="AccountCircle" Width="24" Height="24" Foreground="White"/>
                    </MenuItem.Header>
                    <MenuItem Header="Profile"  Click="NavigateToUserProfile" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Settings" Style="{StaticResource SubMenuItemStyle}"/>
                    <MenuItem Header="Logout" Click="Logout_Profile" Style="{StaticResource SubMenuItemStyle}"/>
                </MenuItem>
            </Menu>
        </DockPanel>
    </materialDesign:ColorZone>
</UserControl>
