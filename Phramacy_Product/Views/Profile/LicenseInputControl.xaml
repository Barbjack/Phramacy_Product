<UserControl x:Class="Phramacy_Product.Views.Profile.LicenseInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="350">
  <UserControl.Resources>
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#DCDCDC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}"  
                                BorderThickness="{TemplateBinding BorderThickness}"  
                                Background="{TemplateBinding Background}"  
                                CornerRadius="5" SnapsToDevicePixels="True">
                            <Grid>
                                <TextBlock x:Name="PlaceholderTextBlock"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#AAA"
                                           FontStyle="Italic"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                           Margin="{TemplateBinding Padding}"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="False"  
                                              HorizontalScrollBarVisibility="Hidden"  
                                              VerticalScrollBarVisibility="Hidden"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsKeyboardFocused" Value="False"/>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="PlaceholderTextBlock" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#B0B0B0"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4B89F2"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePickerStyle" TargetType="{x:Type DatePicker}">
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="BorderBrush" Value="#DCDCDC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4B89F2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"  
                                CornerRadius="5" Padding="{TemplateBinding Padding}"  
                                BorderBrush="{TemplateBinding BorderBrush}"  
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <materialDesign:Card UniformCornerRadius="8" Padding="20" Margin="0,0,0,20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0">
                <TextBlock Text="{Binding LicenseTitle, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                           FontSize="18" FontWeight="SemiBold" Foreground="#0b05ad" Margin="0,0,0,10"/>
                <Label Content="License Number" FontSize="16" Foreground="Black"/>
                <TextBox x:Name="licenseNumberTextBox" Tag="Enter license number" Style="{StaticResource ModernTextBoxStyle}"/>
                <Label Content="Expiry Date" Foreground="Black" Margin="0,10,0,0"/>
                <DatePicker x:Name="expiryDatePicker" Style="{StaticResource ModernDatePickerStyle}"/>
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Grid.RowSpan="3" VerticalAlignment="Center">
                <TextBlock Text="Upload File" HorizontalAlignment="Center" FontWeight="SemiBold" Foreground="Black" Margin="0,0,0,25"/>
                <Border x:Name="fileDropBorder" BorderBrush="#DCDCDC" BorderThickness="1" Height="80" Width="80" CornerRadius="5" 
                        Background="#F8F8F8" Margin="0,0,0,10" AllowDrop="True" 
                        MouseLeftButtonUp="FileDropBorder_MouseLeftButtonUp">
                    <Grid>
                        <TextBlock x:Name="plusSignTextBlock" Text="+" FontSize="30" Foreground="#AAA" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="fileNameTextBlock" Text="" FontSize="10" Foreground="#333" VerticalAlignment="Center" HorizontalAlignment="Center" 
                                   TextWrapping="Wrap" Margin="5" Visibility="Collapsed"/>
                    </Grid>
                </Border>
                <Button Content="{Binding SaveButtonText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                        Margin="0,25,0,0" Style="{StaticResource ModernButtonStyle}" Width="80" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>  