<UserControl x:Class="Phramacy_Product.Views.Profile.StaffContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700">
    <UserControl.Resources>
        <!-- Style for a modern button -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4B89F2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
           
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" 
                          
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#3A75E5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2E5AA3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="10,0">
                <TextBlock Text="Staff Management" 
                           FontSize="22" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,20"/>

                <!-- Placeholder for staff list or a list view -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Background="White" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Manage your staff accounts and their access levels here."
                                       Foreground="#666"
                                       TextWrapping="Wrap"
                                       Grid.Column="0"
                                       VerticalAlignment="Center"/>
                            <Button Content="Add Staff Member"
                                    Style="{StaticResource ModernButtonStyle}"
                                    Grid.Column="1"/>
                        </Grid>

                        <!-- Example list item -->
                        <Border BorderBrush="#DCDCDC" BorderThickness="0,0,0,1" Margin="0,20,0,0" Padding="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="40" Height="40" Fill="#F0F0F0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Color="#E0E0E0" Opacity="0.5" ShadowDepth="2" BlurRadius="5"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="John Doe" FontWeight="SemiBold"/>
                                    <TextBlock Text="Manager" Foreground="#888"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Another example list item -->
                        <Border BorderBrush="#DCDCDC" BorderThickness="0,0,0,1" Margin="0,10,0,0" Padding="0,0,0,10">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="40" Height="40" Fill="#F0F0F0"/>
                                <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="Jane Smith" FontWeight="SemiBold"/>
                                    <TextBlock Text="Pharmacist" Foreground="#888"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
