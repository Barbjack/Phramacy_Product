<UserControl
    x:Class="Phramacy_Product.Views.Cards.MonthlySaleGraph"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:local="clr-namespace:Phramacy_Product.Views.Cards"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Border Margin="10" Padding="10" Background="White" CornerRadius="8"
        BorderBrush="#DDD" BorderThickness="1"
        materialDesign:ShadowAssist.ShadowDepth="Depth1">
        <Grid>
            <lvc:CartesianChart
            Series="{Binding SeriesCollection}"
            DisableAnimations="True"
            Hoverable="False">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis
                    Labels="{Binding Labels}"
                    ShowLabels="True"
                    Title="Month" Foreground="DarkBlue">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="1" />
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis
                    LabelFormatter="{Binding YFormatter}"
                    ShowLabels="True"
                    Title="Sales" Foreground="DarkBlue"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
        </Grid>
    </Border>
</UserControl>
    