<UserControl x:Class="Phramacy_Product.Views.Cards.ExpiringItemCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:local="clr-namespace:Phramacy_Product.Views.Cards"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Border Margin="10" Padding="10" Background="White" CornerRadius="6"
            BorderBrush="#DDD" BorderThickness="1"
            materialDesign:ShadowAssist.ShadowDepth="Depth1">
        <StackPanel>
            <TextBlock Text="Expiring Items" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />

            <ItemsControl ItemsSource="{Binding FilteredExpiringItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Grid Margin="0,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                                    <TextBlock  FontSize="12" Foreground="Gray">
                                        <Run Text="Qty:"/>
                                        <Run Text="{Binding Quantity}"/>
                                    </TextBlock>
                                </StackPanel>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding ExpiryDate, StringFormat='MM/yy'}"
                                           Foreground="Red"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Right" />
                            </Grid>
                            <Border Height="1" Background="#EEE" Margin="0,4"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Grid Margin="0,8,0,0">
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <Button Background="DarkBlue" Content="&lt;&lt;" Click="FirstPageClick" Margin="0,0,5,0"/>
                    <Button Background="DarkBlue" Content="&lt;" Click="PreviousPageClick" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" Margin="5,0"/>
                    <TextBlock Text="/" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" Margin="5,0"/>
                    <Button Background="DarkBlue" Content=">" Click="NextPageClick" Margin="5,0,5,0"/>
                    <Button Background="DarkBlue" Content=">>" Click="LastPageClick"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>