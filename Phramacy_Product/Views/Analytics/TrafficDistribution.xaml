<UserControl x:Class="Phramacy_Product.Views.Analytics.TrafficDistribution"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:local="clr-namespace:Phramacy_Product.Views.Analytics"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"  
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Margin="10" Padding="20" Background="White" CornerRadius="8"
            BorderBrush="#DDD" BorderThickness="1"
            materialDesign:ShadowAssist.ShadowDepth="Depth1">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Organic vs Paid Search" FontSize="16" Foreground="Black" FontWeight="SemiBold"/>
                    <TextBlock Text="Traffic Distribution" FontSize="20" Foreground="DarkBlue" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <materialDesign:PackIcon Kind="ArrowUp" Foreground="Green" VerticalAlignment="Center" Margin="0,0,2,0"/>
                        <TextBlock Text="{Binding IncreaseValue, StringFormat=₹{0:F2}}" Foreground="Green" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <Ellipse Fill="#E57373" Width="12" Height="12" VerticalAlignment="Center"/>
                        <TextBlock Text="Organic" Margin="8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <Ellipse Fill="#66BB6A" Width="12" Height="12" VerticalAlignment="Center"/>
                        <TextBlock Text="Direct" Margin="8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,2">
                        <Ellipse Fill="#42A5F5" Width="12" Height="12" VerticalAlignment="Center"/>
                        <TextBlock Text="Paid" Margin="8,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Canvas x:Name="ChartCanvas" Width="250" Height="250"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>