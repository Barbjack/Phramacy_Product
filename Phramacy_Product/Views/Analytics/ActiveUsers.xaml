<UserControl x:Class="Phramacy_Product.Views.Analytics.ActiveUsers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Phramacy_Product.Views.Analytics"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Margin="10" Padding="15" Background="White" CornerRadius="8"
            BorderBrush="#DDD" BorderThickness="1"
            materialDesign:ShadowAssist.ShadowDepth="Depth1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
             <DockPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock Text="Active Users" FontWeight="Bold" FontSize="18" DockPanel.Dock="Left"/>
                <materialDesign:PackIcon Kind="DotsHorizontal" Grid.Column="1" HorizontalAlignment="Right" DockPanel.Dock="Right" VerticalAlignment="Top" Foreground="#AAA"/>
            </DockPanel>

            <Grid Grid.Row="0" Margin="0,40,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="User" FontWeight="SemiBold" FontSize="14" Grid.Column="0"/>
                <TextBlock Text="No Of Bills" FontWeight="SemiBold" FontSize="14" Grid.Column="1"/>
            </Grid>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Users}" MinHeight="420" >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="35" Height="35" CornerRadius="100" 
                                 Background="{Binding AvatarColor}" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Initial}" 
                                    Foreground="White" FontWeight="Bold"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CustomerName}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding MobileNumber}" FontSize="12" Foreground="Gray"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2" Text="{Binding noOfbills}" 
                                Foreground="Red" FontWeight="SemiBold" FontSize="14"
                                VerticalAlignment="Center"/>
                            </Grid>
                            <Border Height="1" Background="#EEE" Margin="0,4"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="2" Text="No of Bills of each customer within Month." HorizontalAlignment="Center" Margin="0,15,0,0"
                       Foreground="Red" FontSize="15"/>
        </Grid>
    </Border>
</UserControl>
