<UserControl x:Class="Phramacy_Product.Views.Analytics.ActiveUsers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Phramacy_Product.Views.Analytics"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Border Margin="10" Padding="15" Background="White" CornerRadius="8"
            BorderBrush="#DDD" BorderThickness="1"
            materialDesign:ShadowAssist.ShadowDepth="Depth1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Active Users" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <materialDesign:PackIcon Kind="DotsHorizontal" Grid.Column="1" VerticalAlignment="Center" Foreground="#AAA"/>
            </Grid>

            <ListBox Grid.Row="1" ItemsSource="{Binding Users}" BorderBrush="Transparent" Background="Transparent" Padding="0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0,10"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0" Margin="0,0,10,0" VerticalAlignment="Center">
                                <Ellipse Width="40" Height="40" StrokeThickness="2">
                                    <Ellipse.Fill>
                                        <ImageBrush ImageSource="{Binding ImageUrl}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                         Margin="-2,-2,0,0" Fill="Green" Stroke="White" StrokeThickness="2"/>
                            </Grid>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock Text="{Binding Role}" Foreground="#888" FontSize="12"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Grid.Row="2" Text="All active users >" HorizontalAlignment="Center" Margin="0,15,0,0"
                       Foreground="#7C92FF" Cursor="Hand" TextDecorations="Underline"/>
        </Grid>
    </Border>
</UserControl>
